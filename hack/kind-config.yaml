# kind cluster configuration for AKIL development
# Creates a 3-node cluster to test DaemonSet scheduling behavior.

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: akil
nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker
